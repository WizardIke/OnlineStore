@model _16125296_OnlineStore.ViewModels.BasketSummaryViewModel

<ul class="nav navbar-nav navbar-right">
    <li>
        @Html.ActionLink("Your Basket: "+ Model.NumberOfItems + (Model.NumberOfItems == 1 ? " item" : " items") + " " + HttpUtility.HtmlDecode(
       Html.DisplayFor(model=>Model.TotalCost).ToString()), "Index", "Basket")
    </li>
</ul>
